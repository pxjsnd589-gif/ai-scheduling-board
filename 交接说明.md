# AI 排期系统 —— 交接说明

> 交接人：彭炫境（ppxjpeng）　|　接手人：李俞奇（yorickli）
> 日期：2026-08-14

## 一、这是什么

**世界观支持需求的排期工具。** 填需求 → 自动分类、算优先级、推荐负责人、扣假期、
排出每个环节的起止日期 → 导出彩色甘特图 Excel（可直接贴进 TAPD）。

已上线，内网打开就能用，**不用装任何东西**：

**https://ai-scheduling-board.pages.woa.com/**

（内网免登录。数据存在浏览器本地，不上传服务器，各人用各人的。）

**管理权限已经给你了**：管理员名单是 `ppxjpeng` + `yorickli`，
管理页 https://pages.woa.com/admin/ai-scheduling-board.pages.woa.com 可以直接进。

---

## 二、先看这份

📗 **`文档/排期工具_维护手册.xlsx`** —— 遇到任何问题先翻它。

它按「遇到什么事 → 怎么办」组织，5 页：

| 页 | 内容 |
|---|---|
| 先读这个 | 网站在哪、数据存哪、要不要维护 |
| 常见情况 | 14 种常见需求，标了哪些自己就能改、哪些要问 AI |
| 问 AI 模板 | 7 段可直接复制的话术，粘给 AI 就能改程序 |
| 出问题了 | 按现象查处理办法 |
| 包里有什么 | 每个文件是干什么的 |

**核心结论：绝大多数调整在网站「设置」页直接就能改，不用碰程序。**
包括工时、流程环节、审批人、优先级档位、人数，以及**法定假期**。

---

## 三、日常什么都不用做

网站是静态网页，没有后台程序、没有数据库、不依赖任何外部服务。
它不会自己坏，不需要日常维护。

只有两种情况需要动它：

1. **业务规则变了** —— 放假安排、工期、流程环节、需求分类
2. **需求收集表格结构变了** —— 加了新列、改了列名

除此之外可以完全不管。

---

## 四、法定假期怎么更新（新功能）

国务院一般在前一年 11-12 月发布次年放假安排。更新方法：

1. 打开网站 →「设置」→ 拉到最下面「法定假期」
2. 点「➕ 添加新一年的假期」
3. 按弹窗三步走：
   - 复制那段话，去问任意 AI（元宝／豆包／DeepSeek 都行）
   - 把 AI 的回答粘贴回来
   - 点「预览会扣哪几周」，确认后保存

系统会自动算出该跳过哪几周（规则：某一周放假超过 3 天就跳过整周），
并且告诉你每个假期为什么扣、为什么不扣。

**不用改代码，也不需要联网获取。** 内置了 2026-2028 的数据，从 2029 起你自己补录。

---

## 五、包里有什么

```
源码/
  index.html                  ← 网站本体，改程序就改这一个文件
  另外两个 .js                ← 导出 Excel 用的配套文件，不用改
文档/
  排期工具_维护手册.xlsx      ← ★ 先看这个
  排期甘特图_示例.xlsx        ← 导出效果长什么样
  排期工具_维护手册.md        ← 写给程序员看的详细版
  维护速查.md                 ← 同上
  代码架构与逐行注解.md       ← 同上
  代码索引.md                 ← 同上
测试/
  verify_smartsheet.js        ← 改完程序自动检查有没有改坏
  verify_holiday.js           ← 同上（假期功能）
工具/
  表单字段自检.js             ← 改了需求收集表后，检查系统认不认新的列
  gen-code-index.js           ← 给程序员用的辅助脚本
  skill同步核对.js            ← 同上（只有装了 AI 助手版才用得到）
  deploy.js                   ← 发布到内网
AI助手版/                     ← 可选，见第十二节。不用就整个文件夹忽略
交接说明.md                   ← 本文件
```

那几个 `.md` 文件你**不用看**。它们是写给程序员的，
真要改程序时可以连同 `index.html` 一起发给 AI，它看得懂。

`源码/index.html` 双击就能在浏览器打开运行，不需要编译、不需要装依赖。

跑检查脚本的话，进 `测试/` 目录直接 `node verify_smartsheet.js` 就行，
脚本会自己找到源码。

---

## 六、要改程序怎么办

翻手册的「问 AI 模板」页，复制对应话术，连 `源码/index.html` 一起发给 AI。

改完让 AI 告诉你怎么跑 `测试/` 下的检查脚本，都通过再发布。

---

## 七、怎么发布上线

> 只有改了程序才需要这一节。

**一次性准备**：去 https://pages.woa.com/admin 点「申请 API Key」，
复制后在 PowerShell 里执行一次：

```powershell
[Environment]::SetEnvironmentVariable('OA_PAGES_API_KEY','你的Key','User')
```

> Key 绑本人账号，每人一个不能共用。我的会随账号失效，所以不能给你。

**发布命令**（在包的根目录）：

```bash
node 工具/deploy.js update ai-scheduling-board.pages.woa.com ./源码 index.html
```

**发布后一定要验证**，别只看命令有没有报错：

```bash
curl -s https://ai-scheduling-board.pages.woa.com/ -o live.html
diff live.html 源码/index.html && echo "一致，发布成功"
```

浏览器里看记得 **Ctrl+Shift+R 强制刷新**。

不确定的话，用手册「问 AI 模板」页的「改完怎么上线」话术，AI 会一步步教你。

---

## 八、关于外网那份

这个工具还挂在外网 GitHub Pages 上（我个人账号建的），
已经更新到最新版，之后**不会再推送**。

**你只需要维护内网这一份。** 外网那个可以完全忽略。

---

## 九、万一这个包丢了

线上那份就是完整源码（未压缩，注释都在）：

```bash
curl https://ai-scheduling-board.pages.woa.com/ -o index.html
```

只要网站还在，源码就不会真丢。

---

## 十、这个网站会一直在吗

**会。已跟服务负责人确认过。**

OA Pages（pages.woa.com）是公司内网的静态网站服务，负责人是
**xcatliu（刘易成，腾讯集团本部 Doc 组）**。2026-08-10 我直接问过他：

> 我：我离职后账号失效，这个站点会被自动清理吗？
> **他：会一直保留**
>
> 我：owner 字段改不了会一直是我，mentor 能正常管理吗？
> **他：owner 失效不会有影响吧，admin 也有完全权限**

所以：

- 站点不会因为我离职而消失
- `owner` 永久是我（改不了），但不影响你 —— 权限只看管理员名单，你在里面
- 删除站点只有一条路：管理员主动删。不会自动发生
- 站点是公开权限，我账号失效也不影响任何人访问

**唯一要留心的**：站点文件存在内网单机上，官方文档没提异地备份。
「会一直保留」不等于「有备份」，所以**这个包请自己也存一份**（微云／本地都行）。

服务本身的问题可以直接企微找 xcatliu，他人很好、回复很快。

---

## 十一、要提醒业务方的两件事

1. **数据存在浏览器本地。** 清缓存 / 换设备 / 换浏览器 → 数据全没。
   让他们定期点右上角 **📤 导出** 存一份备份。
2. **「重置」按钮不可逆**，会清掉所有需求和任务。

---

## 十二、AI 助手版（可选，不用可以整个忽略）

`AI助手版/` 文件夹里是同一套排期规则的另一份实现，可以让 AI 助手
（WorkBuddy 客户端）直接干活：在对话里说「帮我排个需求」，
它会逐项问你，最后吐出甘特图 Excel。

**网页版已经够用了。这份纯属多给你一个选择，不想折腾就跳过这节。**

它能做网页版做不到的两件事：

1. 在企业微信里直接排期 —— WorkBuddy 客户端可以绑企微，
   绑完在企微给自己发消息就等于在本机下指令
2. 一句话查东西，比如「系统能认哪些表格列」

### 怎么装

1. 装 WorkBuddy 客户端（司内搜一下就有）
2. 把 `AI助手版/` 整个文件夹复制到 `C:\Users\你的用户名\.workbuddy\skills\ai-scheduling\`
3. 重启客户端
4. 对话里说「帮我排个需求」试试

需要电脑上有 Node（`node -v` 能出版本号就行）。

### 几条常用命令

在 `AI助手版/` 目录下开命令行：

```bash
node cli.js "冰龙BOSS场景"           # 开始排期，它会一题一题问
node cli.js --fields                 # 系统能认哪些表格列
node cli.js --check-form 需求表.xlsx  # 我的表格系统认不认
node cli.js --holiday 2029           # 补录假期（会先给你问 AI 的话术）
node selftest.js                     # 自检，应该显示 238 项全 PASS
```

### 两个要注意的

- **假期要在网页版录一遍。** 用 `cli.js --holiday` 录的只在那一次命令里有效，
  网页版的数据存在浏览器里，命令行写不进去。
- **改了网页版，这份不会自动跟着变。** 两份是独立的代码。
  改完网页版跑一下 `工具/skill同步核对.js`，它会告诉你这份哪里没跟上。
  不用 AI 助手版的话，这条完全不用管。

---

## 十三、有问题找谁

| 问题类型 | 找谁 / 看哪 |
|---|---|
| 工具怎么用、要改什么 | `文档/排期工具_维护手册.xlsx` |
| 要改程序 | 手册「问 AI 模板」页，复制话术问 AI |
| 网站打不开、权限问题 | 企微找 xcatliu |
| 实在解决不了 | 找我，离职后微信还在 |
