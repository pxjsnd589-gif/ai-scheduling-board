# AI 排期系统 —— 交接说明

> 交接人：ppxjpeng（彭炫境）　|　接手人：yorickli（李俞奇）
> 打包日期：2026-08-14　|　版本：v75.2

## 一、这是什么

**世界观支持需求的排期工具。** 填需求 → 自动分类、算优先级、推荐负责人、扣假期、
排出每个环节的起止日期 → 导出彩色甘特图 Excel（可直接贴进 TAPD）。

已上线，内网直接打开就能用，**不需要装任何东西**：

**https://ai-scheduling-board.pages.woa.com/**

（内网免登录。数据存在浏览器本地，不上传服务器，各人用各人的。）

**管理权限已经给你了**：站点管理员名单现在是 `ppxjpeng` + `yorickli`，
你已有完全权限，管理页 https://pages.woa.com/admin/ai-scheduling-board.pages.woa.com
可以直接进。

---

## 二、先说结论：日常什么都不用做

网站是纯静态页面，**没有后端、没有数据库、没有定时任务、不依赖任何外部服务**。
它不会自己坏，不需要日常维护。同事直接用上面那个链接就行。

**「维护」只在两种情况下才需要动手**：

1. **业务规则变了** —— 假期表要更新、工期要调、流程环节要加减、分类要改
2. **需求收集表格的结构变了** —— 加了新列、改了列名、换了填写方式

除此之外，你可以完全不管它。

如果只是要让它继续能用，看到这里就够了。下面是要改东西时才需要看的。

---

## 三、包里有什么

```
源码/
  index.html                     ← 全部功能都在这一个文件里（约 6900 行，含中文注释）
  xlsx-js-style.min.js           ← 第三方库，导出 Excel 用，不用改
  fflate.min.js                  ← 第三方库，压缩用，不用改
文档/
  维护速查.md                    ← ★ 先看这个，一页解决「我该动哪里」
  排期工具_维护手册.md            ← 细节：排期规则、分类逻辑、坑位说明、发布流程
  代码架构与逐行注解.md           ← 想深入理解代码时看
  代码索引.md                    ← 函数名 → 行号（自动生成）
  排期甘特图_示例.xlsx            ← 导出效果长什么样
测试/
  verify_smartsheet.js           ← 改解析逻辑后必跑，102 项断言
工具/
  gen-code-index.js              ← 重新生成代码索引
  deploy.js                      ← 发布到内网
交接说明.md                      ← 就是本文件
```

**核心只有一个文件**：`源码/index.html`。它是自包含的单文件网页，
双击就能在浏览器里打开运行。**没有构建步骤、没有 npm install、没有框架。**

> 文件名叫 `index.html` 是因为网站首页必须是这个名字，发布时可以直接用。
> 它在原项目里的名字是 `ai-scheduling-board-v10.html`，内容完全一样。

**建议的阅读顺序**：
1. `文档/维护速查.md` —— 5 分钟，知道遇到问题翻哪一页
2. 真的要改东西时，再去 `文档/排期工具_维护手册.md` 找对应章节
3. `代码架构与逐行注解.md` 只在想搞懂原理时看，平时不用

---

## 四、怎么改

### 1. 改代码

用任意编辑器打开 `源码/index.html`，改完直接双击在浏览器里看效果。**不需要编译。**

**很多需求根本不用改代码** —— 工时、流程环节、审批人、优先级档位、人数
都可以在工具的「设置」页直接改。先确认这一点。

真要改代码，请先读 `文档/维护速查.md`。最容易踩的三个坑：

- **改内置配置要升 `_cfgVersion`**（在 `DEFAULT_CONFIG` 里）。
  不升的话用户浏览器里的旧缓存不会更新，症状是「我改了但业务方那边没变」。
- **新增子分类只加别名，别改 `CATEGORY_TREE` 的 key**。
  这两个地方名字不一致会导致需求被静默判成默认分类，走错整条流程 ——
  不报错，只能靠人肉发现。已经踩过一次。
- **环节名里带「修改」「发包」会被判定为嵌入型节点**（锁死 1 天工期）。
  自定义环节取名要避开这两个词。

### 2. 改完必须跑的三步

```bash
# ① 语法校验（3 秒）—— 挡住"整页白屏"这种致命问题
node -e 'const fs=require("fs"),vm=require("vm");const h=fs.readFileSync("index.html","utf8");const re=/<script\b[^>]*>([\s\S]*?)<\/script>/gi;let m,i=0,b=0;while((m=re.exec(h))){i++;try{new vm.Script(m[1]);}catch(e){b++;console.log("❌ 第"+i+"块:"+e.message);}}console.log("共"+i+"块,错误"+b+"个");'

# ② 回归断言（5 秒）—— 必须输出 FAIL 0
node 测试/verify_smartsheet.js

# ③ 浏览器实测 —— 双击 HTML，上传一份真实表格，看右侧表单填对没有
```

**第③步不能省。** 有过「脚本全绿但真实上传是错的」的先例
（原因是入口处对文本做了 `trim()`，吃掉了表头的前导空列）。
**底层函数通过 ≠ 用户路径通过。**

---

## 五、怎么发布

> **只有改了代码需要重新上线时才看这一节。** 如果只是用工具，
> 或只想改访问权限，都不需要 API Key。

### 1. 一次性准备：申请自己的 API Key

打开 https://pages.woa.com/admin ，点「申请 API Key」，复制（只显示一次）。
然后在 PowerShell 里执行一次：

```powershell
[Environment]::SetEnvironmentVariable('OA_PAGES_API_KEY','你的Key','User')
```

> ⚠️ Key 绑本人账号，**每人一个，不能共用**。我的 Key 会随账号失效，
> 所以不能给你。也不要发到群里或写进文件。

管理员名单已经帮你加好了（`ppxjpeng` + `yorickli`），这一步不用做。

### 2. 发布命令

```bash
node 工具/deploy.js update ai-scheduling-board.pages.woa.com ./源码 index.html,xlsx-js-style.min.js,fflate.min.js
```

只改了 HTML 的话可以只传它，更快：

```bash
node 工具/deploy.js update ai-scheduling-board.pages.woa.com ./源码 index.html
```

### 3. 发布后一定要验证

**别只看命令返回成功。** 实际拉一次线上文件比对字节：

```bash
curl -s https://ai-scheduling-board.pages.woa.com/ -o live.html
diff live.html 源码/index.html && echo "一致，发布成功"
```

> ⚠️ 不要用 `curl -o /dev/null -w "%{size_download}"` 判断，它会误报 0。
> 必须落盘 `diff`。

浏览器里看记得 **Ctrl+Shift+R 强制刷新**，否则看到的是缓存的旧版。

### 4. 还有一个外网入口（可选）

这个工具同时还挂在外网 GitHub Pages 上：
https://pxjsnd589-gif.github.io/ai-scheduling-board/

那是我个人账号建的，**你可以不管它**，内网这份是给组内用的正式入口。

但如果你也想维护外网那份，注意：**改完代码两条链路都要推**，
只推一边会出现「内网是新的、外网还是旧的」。具体命令在维护手册 §10。

---

## 六、万一这个包丢了

**线上那份就是完整源码**（未压缩，注释都在）。任何人都能一条命令取回来：

```bash
curl https://ai-scheduling-board.pages.woa.com/ -o ai-scheduling-board.html
```

所以只要网站还在，源码就不会真的丢。

---

## 七、这个网站会一直在吗

**会。已经跟服务负责人确认过了。**

OA Pages（pages.woa.com）是公司内网的静态网站服务，负责人是 **xcatliu（刘易成，
腾讯集团本部 Doc 组）**。2026-08-10 我直接企微问过他，原话：

> 我：我离职后账号失效，这个站点会被自动清理吗？还是会一直保留？
> **他：会一直保留**
>
> 我：owner 字段改不了会一直是我，这种情况下 mentor 能正常管理吗？
> **他：owner 失效不会有影响吧，admin 也有完全权限**

所以结论很明确：

- **站点不会因为我离职而消失**，会一直保留。
- **`owner` 字段永久是我（改不了），但这完全不影响你。** 鉴权只看 `admin` 名单，
  admin 有完全权限。你在 admin 里就能做任何操作。
- 删除站点只有一条路径：管理员主动调删除接口。不会自动发生。
- 站点权限是「公开」，鉴权不检查用户身份，所以我的账号失效也不影响任何人访问。

**唯一还需要留个心的地方**：站点文件存在内网单机上，官方文档没提到异地备份机制。
「会一直保留」不等于「有备份」。所以这个交接包请你自己也存一份
（微云 / 本地都行），万一哪天机器出问题，源码还在你手上。

有关这个服务本身的任何问题，可以直接企微找 **xcatliu**，他人很好，回复很快。

---

## 八、需要提醒业务方的两件事

1. **数据存在浏览器本地，不在服务器上。** 清缓存 / 换设备 / 换浏览器 → 数据全无。
   让他们定期用工具右上角的 **📤 导出** 按钮存一份 JSON 备份。
2. **「重置」按钮不可逆**，会清掉所有需求和任务，只保留默认配置。

---

## 九、还有一个可选的东西：AI 助手版

除了网页版，同一套排期规则还蒸馏了一份能给 AI 助手（WorkBuddy）调用的版本，
装上以后可以直接在对话里说「帮我排个需求」，它会按 16 题逐项问你，最后吐出甘特图，
并把规范名称直接打在对话里，可以直接复制进 TAPD。

**已同步到 v75.2，功能与网页版一致**，自检 184 项断言全部通过。

唯一的门槛：它需要装 WorkBuddy 客户端 + Node 环境，不像网页版开箱即用。

**如果只是想要个能用的排期工具，用网页版就够了，可以完全忽略这一节。**
想在聊天里直接排期，再找我拿这份 skill。

> 注意：如果你以后既维护网页版又维护 AI 助手版，**改了一边要同步另一边**，
> 否则两边规则会漂移。对应关系在维护手册 §11。

---

## 十、有问题找谁

- **「我该动哪里」** → `文档/维护速查.md`（一页速查表）
- **排期规则细节**（怎么分类、优先分怎么算、假期怎么扣）→ `文档/排期工具_维护手册.md`
- **想搞懂代码怎么写的** → `文档/代码架构与逐行注解.md`
- **找某个函数在第几行** → `文档/代码索引.md`
- **OA Pages 服务本身的问题** → 企微找 xcatliu
- **其它** → 找我，我离职后微信还在
