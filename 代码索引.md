# 代码索引（自动生成，不要手改）

> 由 `scripts/gen-code-index.js` 自动生成 | 生成时间：2026-08-14
> 源文件：`ai-scheduling-board-v10.html`（共 6938 行）

**改完代码请重新跑一次**：`node scripts/gen-code-index.js`

---

## 为什么有这份文件

交接文档里曾经直接写死行号（如「autoSchedule 在 2041 行」）。
代码一改行号就漂移，跳过去看到的是别的东西 —— 比没有行号更误导。
所以行号统一放在这里自动生成，其它文档只提函数名。

**在编辑器里定位的最快方式**：`Ctrl+F` 搜 `function 函数名`，
比翻行号更可靠（行号可能因为你自己的改动而变）。

---

## 配置层（改规则先看这里）

> 静态权威数据。改这里的结构必须把 _cfgVersion +1，否则用户浏览器旧缓存不更新。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 891 | `DEFAULT_CONFIG` | — |
| 1029 | `ASSETS_TREE` | 腾讯问卷的"已有产出物"3 级树（与问卷结构一一对应） 大分类 -> 二级分类 -> 具体项 |
| 1048 | `PIPELINE_REQUIRED` | 管线 → 必做产出项（按矩阵 √ 部分） key: 管线 name（与 config.pip… |
| 1087 | `PIPELINE_OPTIONAL` | 管线 → 可选项（蓝底=选择性，提需人勾选特殊需求才做） 矩阵蓝底仅出现在：Bible概念列… |
| 1111 | `DEFAULT_ITEM_TIME` | 各产出项工期表 (人天) 格式: { min, max, unit, per } 部分文学项… |
| 1604 | `MIDS_WITH_ORDER` | 按生产顺序前缀展开规则：只对美术类 mid 启用（勾 N = 0..N 全勾） 文学类（Bi… |
| 1626 | `CATEGORY_TREE` | 需求分类树（对应腾讯问卷 Q2/Q3/Q4/Q5） ⚠️ key 是【规范名】，必须与下拉框… |
| 1635 | `CATEGORY_SUB_ALIASES` | 子分类别名表（一个 category id 可以有多种历史/口头写法） 来源差异：腾讯问卷选… |
| 1746 | `STORAGE_KEY` | — |

## 分类与子分类归一

> 新增子分类写法只往 CATEGORY_SUB_ALIASES 加别名，永远不要改 CATEGORY_TREE 的 key。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 1612 | `isStrictPipelineName` | 严格管线名匹配（仅用于判断"某个列名是不是管线列"） ⚠️ 不能用 resolvePipel… |
| 1652 | `resolveSubCategoryId` | 子分类文本 → category id |
| 1675 | `canonicalSubName` | 子分类文本 → CATEGORY_TREE 的规范 key（供 record.subScen… |
| 3220 | `resolveCategory` | 类别 → 内部 category id ⚠️ 一律走 resolveSubCategoryI… |
| 3234 | `resolvePipelineId` | 管线名称 → 内部 id（容错匹配） 问卷选项常带前缀（如"世界观Bible概念"），con… |
| 3252 | `resolvePipelineIds` | 管线名称数组 → id 数组（去重、去空） |

## 工时与产出物计算

> 工期估算的源头。工时统一用 daysMin/daysMax 区间，排期取上限，下限仅标注。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 1172 | `getItemTime` | 获取工期默认值(中档) |
| 1260 | `nodeDaysRange` | ============ 工时统一为「范围」表示 ============ 用户规则（v70… |
| 1274 | `rangeText` | 区间 → 显示文本："1~1" / "0.5~1"（统一都带 ~，让"固定"也看得出是区间） |
| 1279 | `upgradeNodeDaysField` | 把节点上的 days旧字段就地升级为 daysMin/daysMax（读取旧缓存时调用，幂等… |
| 1539 | `nodeDays` | 计算 flow 节点的实际工期：litBundle → 各项相加 |
| 3277 | `expandAssetTokens` | 工具：把一组原始词智能展开成具体项 业务规则：每个 mid 内部按生产顺序排列（见 ASSE… |
| 3587 | `findAssetMid` | 字段名 → ASSETS_TREE 里的 {big, mid} |
| 3604 | `findBigOfAssetItem` | 具体产出项名 → 它属于哪个 big（'美术' / '文学'） |
| 4115 | `getAssetPath` | 资产叶子项 → 完整路径 {big, mid, item} |

## 流程引擎

> 排期链的第一环。取名逻辑的数据源必须用 expandedFlowNodes，别用 calculateRemaining（两套逻辑会漂移）。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 1330 | `artItemAllowedForCategory` | 美术产出物按需求分类过滤（用户规则 2026-07-21）： 此规则针对【所有产出物】—— … |
| 1349 | `litItemsForReq` | 某需求实际要做的「文学产出项」清单（必做 + 勾选的选做，仅文学大类）， 按 ASSETS_… |
| 1365 | `buildLitNodes` | 文学产出清单 → 流程节点：Bible 合并 1 个（无序，工时相加） |
| 1386 | `detectArtMids` | 扫描该需求实际会产出的美术二级分类（场景原画 / 角色原画） 注意：与排期引擎一致，按分类过… |
| 1406 | `hasExistingLit` | 提需人是否已填「已有文学设定」：仅特定产出物可豁免对应强制文学设定环节 · 场景豁免项：场景… |
| 1416 | `computeForcedLit` | 计算该需求应强制前置的文学设定节点（返回 [{item, name, type:'scene… |
| 1434 | `expandedFlowNodes` | 展开后的实际流程节点（把 litExpand 占位节点替换成动态文学节点） v46: 当 l… |
| 1562 | `nodeDisplayName` | 流程名 + 工时标注：litBundle → "Bible设定（xxx、yyy）" |
| 1882 | `calculateRemaining` | 根据管线 × 已有产出 × 特殊需求，计算待做清单 + 工期 返回: { required:… |
| 2135 | `nodeMakeRole` | v10 按工种分池：判断制作节点属于哪个工种座位池（lit / sceneArt / cha… |

## 决策链（分类→审批人→优先级）

> 排期链第二环。优先分、重要分、档位判定、审批人推荐都在 runDecisionChain 里，是一整个大函数。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 1598 | `getPipeName` | 管线 id → name 查找 |
| 1870 | `getPipeline` | — |
| 1968 | `runDecisionChain` | — |
| 2171 | `resolveReqReviewer` | 用户规则：整条需求的唯一审核人（内部重要程度总分 → 分级 → 具体人名） ① 特殊：管线含… |

## 排期引擎（核心算法）

> review 节点锁 1 天并嵌入前一制作环节；节点名含「修改」「发包」会被判为嵌入节点，自定义环节取名要避开。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 1690 | `getHolidayWeeks` | — |
| 1696 | `isHolidayWeek` | — |
| 1707 | `getISOWeek` | — |
| 2123 | `resolveOwnerId` | — |
| 2143 | `roleSeatCount` | 某工种的座位数（复用人力配置，与"人力不足"检测同一套：lit3 / sceneArt2 /… |
| 2150 | `pickSeat` | 在某工种座位池里挑"最早空闲"的座位ID（座位ID = role#index，如 scene… |
| 2190 | `_compactSeatDelayGap` | ===== 排期引擎的三个后处理步骤（从 runPass 中拆出，逻辑不变，便于维护）===… |
| 2219 | `_recomputeSeatOccupancy` | 后处理②：压缩后重算各座位占用（只会变晚，不会变早）→ 保证跨需求座位账目准确 |
| 2228 | `_stretchMakeBeforeMeeting` | 后处理③：填补评审/周会前的空档 —— 把评审前那个制作节点的结束日拉长到评审前一个工作日，… |
| 2247 | `autoSchedule` | — |
| 4937 | `batchSchedule` | — |
| 5174 | `_doBatchSchedule` | — |
| 6198 | `_isWorkday` | — |
| 6199 | `_nextWorkday` | — |
| 6202 | `_addWorkdays` | 从 start（含）起第 n 个工作日（n>=1，start 计为第1个） |
| 6207 | `_latestStartDate` | 计算一条需求的"最晚开始日期" = DDL 往前倒推 (制作+评审+缓冲 的工作日总数) t… |

## 表格导入解析（问卷 / 智能表格）

> 列语义判定优先级固定为「字段名 > 数据内容 > 组名」。组名最不可信，不要改回组名优先。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 2882 | `tableToTabText` | 把 2D 数组（含表头）转成 tab 分隔字符串，给 parseQuestionnaireM… |
| 2899 | `detectTextEncoding` | 读取 csv / txt 文件并解析为 2D 数组（首行表头） 表头同样保留原始文本（理由见… |
| 2940 | `readCsvFile` | — |
| 2976 | `readXlsxFile` | 读取 xlsx 文件并解析为 2D 数组（首行表头） ⚠️ 关键：表头必须保留【原始文本】（… |
| 3024 | `xlsxCellToText` | xlsx 单元格 → 文本 |
| 3039 | `sniffFileKind` | 读文件头几个字节，判断真实格式（不信后缀名） 用户经常把 xlsx 改名成 .xls，或者把… |
| 3157 | `normalizeHeader` | 规范化表头：去除前缀数字、空格、矩阵类的"基础名:选项"后缀 |
| 3168 | `stripMatrixPrefix` | 去掉腾讯问卷选项前缀 "A.英雄线" → "英雄线" |
| 3178 | `splitMulti` | 多选字段拆分：兼容 \n、,、，、 |
| 3193 | `splitAssets` | 资产字段专用拆分：识别腾讯问卷 "[场景原画] 概念方向草图" 这种格式 规则： 1) 先去… |
| 3437 | `detectQuestionnaireFormat` | 检测是否为腾讯问卷多列格式 |
| 3456 | `extractBracketItem` | 从表头原始文本提取 bracket 后的项名 "9.文学已有产出具体项:[Bible设定] … |
| 3464 | `extractColonSuffix` | 从表头提取冒号后的简单后缀（无 bracket） "11.是否有特殊产出需求:Bible设定… |
| 3476 | `parseCopyInfo` | v44: 解析「需求副本信息」字段 格式：每份副本 "名称,DDL"，多份用 ; 或 | 或… |
| 3492 | `normalizeDDL` | v44: 统一 DDL 为 YYYY-MM-DD（兼容 2026/9/30 与 2026-0… |
| 3618 | `sniffColumnKind` | 值嗅探：靠该列的实际数据判断列语义（表头不认识 / 组名不可靠时的兜底） 只在「样本非空且全… |
| 3639 | `resolveSubCategory` | 子分类值归一：把单元格值对到 CATEGORY_TREE 的**规范 key** ⚠️ 必须… |
| 3648 | `detectSmartsheetLayout` | 探测是否为智能表格布局 |
| 3672 | `buildSmartsheetPlan` | 生成列解析计划：每列 → null（忽略）或 {col, kind, ...} ⚠️ 组名行… |
| 3760 | `parseSmartsheetTable` | 主解析：智能表格 2D 表 → 记录数组（字段结构与 parseQuestionnaireM… |
| 3840 | `expandRecordCopies` | v44 需求副本展开（问卷 / 智能表格两条链路共用） needCopy 含「是/需要」且 … |
| 3860 | `detectImportFormat` | 统一格式探测：智能表格 / 腾讯问卷多列 / 简单行列 |
| 3872 | `parseQuestionnaireMultiCol` | 解析腾讯问卷多列格式 注意：cell 内的换行（多选用 \n 分隔）会被错误切分成多行， 因… |
| 3972 | `parseSimple` | 解析简单格式（按行/按列的旧格式） |
| 4003 | `aiParseAndFill` | — |
| 4257 | `recordToReq` | 把 record 转为可入库的 req 对象（包含决策链所需的全部字段） |
| 4409 | `parseTextToTable` | 把任意纯文本解析成二维数组（tab/逗号分隔） |

## 命名与甘特图导出

> 甘特图加列时必改 4 处：LEFT 数组、C_xxx 列常量、st.cols 列宽、dataValidation.col。规范名称顺序是 ①文学设定 ②美术。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 4138 | `litNodeToAsset` | ===== 规范名称生成（2026-07-22 定稿）===== 公式：{产出类型} _ {… |
| 4152 | `generateReqNames` | — |
| 4235 | `renderReqNamesBlock` | 生成弹窗内的「规范名称」区块 HTML |
| 6217 | `rescheduleAllForExport` | 导出前重跑决策链 + 重排（保证 Excel 反映最新 flows / 假期 / 优先级） |
| 6269 | `_exportComputeCapacity` | ===== 导出 Excel 的四个子步骤（从 exportExcelGantt 中拆出，逻… |
| 6313 | `_exportBuildInfoSheet` | 子步骤B：Sheet2「优先级&审批」 |
| 6342 | `_exportBuildHolidaySheet` | 子步骤C：Sheet3「假期扣除周」 |
| 6356 | `_exportBuildManpowerSheet` | 子步骤C2：Sheet「人力负荷」——按工种(文学/场景原画/角色原画)统计制作人力占用， … |
| 6420 | `_exportInjectFreezeDropdown` | 子步骤D：后处理——注入冻结窗格 + 状态列下拉（xlsx-js-style 不支持，用 f… |
| 6456 | `exportExcelGantt` | — |

## 界面渲染与表单

> 排期前的 4 类追问弹窗（量级/开发模式/角色卡/概念草图）都在 runPreScheduleModals。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 2471 | `switchView` | — |
| 2492 | `renderInputForm` | 渲染: 需求录入 |
| 2505 | `renderAssetsPicker` | 渲染"已有产出物"3 级折叠展开多选树 |
| 2589 | `getSelectedAssets` | 读取当前 picker 的选中项 |
| 2594 | `setSelectedAssets` | 设置 picker 选中项 |
| 2632 | `renderSpecialPicker` | 特殊需求选择器：根据当前选中的管线，显示命中可选项 |
| 2719 | `getSelectedSpecialItems` | — |
| 2725 | `setSelectedSpecialItems` | — |
| 3366 | `fillOneRecord` | 解析单条记录（已映射的字段对象）并填表 |
| 4065 | `clearFormFields` | 清空右侧表单所有字段（不弹确认） |
| 4079 | `readCurrentFormToRecord` | 把当前表单内容回写为 record 对象（用于切换条目时保存编辑） |
| 4297 | `renderPendingQueue` | 渲染队列 UI |
| 4346 | `selectPendingRecord` | 切换到队列第 idx 条 |
| 4450 | `buildSingleReq` | 从当前表单构建需求对象 |
| 4467 | `submitRequirement` | — |
| 4485 | `submitBatch` | 批量提交：跑所有队列记录的决策链 |
| 4846 | `renderPool` | 渲染: 需求池 |
| 5163 | `runPreScheduleModals` | v69: 依序串起多个弹窗收集步骤，每一步有问题时才弹对应弹窗 |
| 5459 | `renderSettings` | — |
| 5660 | `renderFlowsEditor` | — |
| 6094 | `aiSend` | — |

## 状态持久化

> 数据存浏览器 localStorage，不上服务器，各人用各人的。改内置 flows/holidays 结构要把 _cfgVersion +1。

| 行号 | 名称 | 作用 |
|---:|---|---|
| 1748 | `loadState` | — |
| 1793 | `saveState` | — |
| 1803 | `_mergeUserFlowNodes` | 配置版本升级时合并 flows：内置节点取最新版（权威工时/规则），用户自建节点按相对位置补… |
| 1856 | `getReq` | — |
| 1859 | `normalizeReq` | 单一入口：所有"新建 req 对象"都过一遍这里，保证排期必需字段齐全， 杜绝某条代码路径漏… |
| 1869 | `getPerson` | — |
| 4776 | `_saveReqToPool` | — |

---

## 统计

- 索引条目：119 个
- 源文件行数：6938
