# AI 排期工具 · 代码结构 & 维护手册

> 适用对象：接手维护本工具的同事（世界观组）
> 当前版本：`ai-scheduling-board-v10.html`，`_cfgVersion: 70`，源码约 6900 行
> 最后更新：2026-08-14
> 维护原则：**所有修改只动这一个文件**，其余都是它的副本或依赖。
>
> **配套文件**：
> - `代码索引.md` —— 函数名 → 行号对照表，**自动生成**，改完代码跑
>   `node scripts/gen-code-index.js` 刷新。本手册只提函数名不写行号（原因见 §8 坑位⑨）。
> - `verify_smartsheet.js` —— 解析逻辑回归测试，改完必跑（见 §9）。

---

## 1. 系统概览

| 项 | 说明 |
|----|------|
| 形态 | 纯前端**单文件 HTML**，无后端、无数据库、无构建步骤 |
| 用途 | 需求录入 → 智能决策（分类路由/审批人/优先级）→ 自动排期 → Excel 导出 |
| 数据存储 | 浏览器 `localStorage`（单 key：`ai_schedule_tool_v1`） |
| 技术栈 | 原生 JavaScript（ES6）+ SheetJS（`xlsx-js-style`，本地引入）；**无框架、无 npm 依赖** |
| 运行环境 | 任意现代浏览器（Chrome/Edge/Firefox），无需 Node、无需联网 |
| 外部依赖 | `xlsx-js-style.min.js`、`fflate.min.js`（**必须与 HTML 同目录**） |
| 线上入口 | **内网** ai-scheduling-board.pages.woa.com ＋ **外网** GitHub Pages（两条都要推，见 §10） |

核心能力：上传/粘贴需求表格（**腾讯文档智能表格** 或 腾讯问卷导出，自动嗅探格式）
→ 自动识别字段 → 生成待提交队列 → 一键批量排期 → 导出带甘特图与优先级表的 Excel。

---

## 2. 文件清单（目录：`2026-07-03-15-54-43/`）

| 文件 | 角色 | 维护动作 |
|------|------|----------|
| **`ai-scheduling-board-v10.html`** | ⭐ 唯一源码 | 所有功能改动都在这 |
| `xlsx-js-style.min.js` | Excel 解析/导出依赖 | 一般不动；升级 SheetJS 时替换 |
| `fflate.min.js` | SheetJS 压缩依赖 | 同上 |
| `deploy/index.html` | **两条发布链路的源** | 改完主文件必须 `cp` 同步过来，否则发的是旧版 |
| `verify_smartsheet.js` | 解析逻辑回归测试 | 改解析逻辑后必跑 |
| `代码索引.md` | 函数名→行号（自动生成） | 跑 `node scripts/gen-code-index.js` 刷新 |
| `scripts/gen-code-index.js` | 索引生成器 | 重命名函数后需更新其中 `GROUPS` |
| `scripts/pre-release-check.js` | 发布前脱敏检查 | 对外分享前跑一次 |
| `dist/index.html` | 历史构建副本（已停用） | 忽略，可删 |
| `ai-scheduling-board.OLD.html` | 早期骨架版（无 `_cfgVersion`） | ❌ 干扰文件，别打开、别改 |
| `.git` | git 仓库 | 外网发布用（`gh-pages` 分支） |

> 根目录和 `deploy/` 下都放了那两个 `.js` 依赖，本地直接双击 HTML 就能用。
> 如果哪天报 `XLSX is not defined`，检查是不是把 HTML 单独拷到别处了。

---

## 3. 运行与部署

### 3.1 本地运行（调试/日常使用）
直接用浏览器打开 `ai-scheduling-board-v10.html` 即可。
> 前提：同目录存在 `xlsx-js-style.min.js` 和 `fflate.min.js`。

### 3.2 部署
**两条链路都要推**，详见 §10。简版：

```bash
cp ai-scheduling-board-v10.html deploy/index.html    # 第一步，别忘
# 然后分别推内网（API）和外网（gh-pages），两边都落盘 diff 验证
```

### 3.3 数据备份 / 迁移
- 工具右上角 **📤 导出** 按钮：把 `requirements + tasks + config` 导出为 JSON 文件（备份用）
- **📥 JSON** 按钮：从 JSON 恢复
- 重置（🔄）会**清空全部需求与任务**，仅保留默认配置，慎用。

---

## 4. 代码组织架构

**行号请查 `代码索引.md`**（自动生成，跑 `node scripts/gen-code-index.js` 刷新）。
本手册刻意不写行号 —— 写死的行号会随代码改动漂移，
跳过去看到别的东西比没有行号更误导（详见 §8 坑位⑨）。

在编辑器里定位最快的方式：`Ctrl+F` 搜 `function 函数名`。

代码按下面的顺序分层，索引文件也是按同样的分组组织的：

| # | 层 | 关注点 | 代表标识符 |
|---|---|---|---|
| A | 配置层 | 静态权威数据，`_cfgVersion` 控制升级 | `DEFAULT_CONFIG` `ASSETS_TREE` `CATEGORY_TREE` `CATEGORY_SUB_ALIASES` `PIPELINE_REQUIRED` `DEFAULT_ITEM_TIME` |
| B | 分类归一 | 各种写法的分类名 → 内部 id | `resolveCategory` `resolveSubCategoryId` `canonicalSubName` `isStrictPipelineName` |
| C | 工时计算 | 产出项 → 人天区间 | `getItemTime` `nodeDays` `nodeDaysRange` `expandAssetTokens` |
| D | 流程引擎 | 需求 → 该做哪些环节 | `expandedFlowNodes` `buildLitNodes` `computeForcedLit` |
| E | 决策链 | 分类 → 审批人 / 优先级 / 档位 | `runDecisionChain` `resolveReqReviewer` |
| F | 排期引擎 | 环节 → 具体起止日期 | `autoSchedule` `batchSchedule` `pickSeat` `_addWorkdays` |
| G | 导入解析 | 表格文件 → 需求记录 | `detectImportFormat` `parseSmartsheetTable` `parseQuestionnaireMultiCol` `readXlsxFile` |
| H | 命名与导出 | 需求 → 规范名 / 甘特图 Excel | `generateReqNames` `litNodeToAsset` `exportExcelGantt` |
| I | 界面渲染 | DOM 操作、表单、弹窗 | `renderInputForm` `renderPendingQueue` `runPreScheduleModals` `renderSettings` |
| J | 状态持久化 | localStorage 读写、字段迁移 | `loadState` `saveState` `normalizeReq` |

**排期主链路（理解代码从这条线入手）**：

```
用户上传表格
   ↓  G 导入解析
需求记录（含分类/管线/已有产出/DDL）
   ↓  B 分类归一 → 得到 category id
   ↓  E 决策链 → 得到优先级、审批人
   ↓  D 流程引擎 → 展开成环节列表（这一步决定"要做什么"）
   ↓  F 排期引擎 → 给每个环节算出起止日期（这一步决定"什么时候做"）
   ↓  H 命名与导出
甘特图 Excel
```

三个关键函数就是 `expandedFlowNodes` → `runDecisionChain` → `autoSchedule`。
**改排期行为，99% 的情况是改这三个之一。**

## 5. 数据模型

### 5.1 `state`
```js
state = {
  config,            // 见 5.3
  requirements: [Req],
  tasks: [Task],
  expandedGroups: {},
  currentView: 'input' | 'settings',
  filter: {...},     // 看板筛选条件
  _pendingRecords: [],   // AI 解析后的待提交队列
  _pendingIdx: 0,
  ...视图状态(loadView/offset/选中等)
}
```

### 5.2 需求对象 `Req`
| 字段 | 含义 |
|------|------|
| `id` | uuid |
| `name` | 需求名 |
| `category` | 系统分类（`scene-story`/`scene-world`/`char-key`/`char-normal`/`lit-simple`/`lit-system`） |
| `subLit` | 文学子分类（B.系统性设定 等） |
| `ddl` | 截止日期 `YYYY-MM-DD`（导出时照搬） |
| `requester` / `owner` | 提需人 / 负责人 |
| `assets` / `assetsLitItem` / `assetsArtItem` | 已有产出物 |
| `specialItems` / `specialCategories` | 特殊需求 |
| `pipelines` | 应用管线 id 数组 |
| `needCopy` / `copyInfo` / `_copyOf` | 副本相关（见 7.7） |
| `status` | 待开始/进行中/完成/超期 |
| `prioScore` / `importanceScore` / `approver` | 优先级总分/重要度/审批人 |
| `nodes` | 决策链生成的流程节点（含 ddl、管子项） |

### 5.3 `config`（DEFAULT_CONFIG 的可改副本）
- `pipelines[]`：管线（id/name/score/importance/approver）
- `flows{}`：4+ 类流程节点表（type: make/review/archive/sync；item/qty/days/ownerRule/reviewerRule/litExpand）
- `holidays{}`：分年度假期周（按"放假>3天扣周"）
- `headcount{}`：文学/场景原画/角色原画 可用人数
- `itemTime{}`：用户改过的工时表（未改则回落 DEFAULT_ITEM_TIME）
- `prioThresholds` / `importanceThresholds`：分档断点
- `buffer` / `people` / `loadThresholds`：缓冲/人员/负载阈值

---

## 6. 核心调用链（理解改哪看这）

```
① 单条录入
   录入表单 → submitRequirement() → buildSingleReq()
            → runDecisionChain(req)  ← 分类路由/文学子分类/优先级/审批人
            → showDecisionResult() 弹窗 → _saveReqToPool() 入池

② 批量导入（智能表格 / 腾讯问卷，自动嗅探格式）
   上传/粘贴 → handleAiFile() → readXlsxFile() 或 readCsvFile()
            → aiParseAndFill() → detectImportFormat() 分流：
                 ├─ smartsheet  → parseSmartsheetTable()       ← 现在的主路径
                 ├─ multi-col   → parseQuestionnaireMultiCol()  ← 老问卷，保留兼容
                 └─ simple      → parseSimple()
            → 待提交队列(_pendingRecords，需求副本自动展开)
            → 逐条 selectPendingRecord() → fillOneRecord() 填表
            → 批量 submitBatch() → runDecisionChain → 入池

③ 排期
   需求池勾选 → batchSchedule() → runPreScheduleModals()
                （问量级/开发模式/角色卡类型/概念草图，共 4 类追问）
            → _doBatchSchedule() → 逐条 autoSchedule() [多轮压缩]
            → 写 state.tasks

④ 导出
   exportExcelGantt() → rescheduleAllForExport()（重排到工作日）
            → generateReqNames() 生成「规范名称」列
            → _exportBuildInfoSheet()（优先级&审批，含 PM单独拉起列）
            → _exportBuildHolidaySheet() / _exportBuildManpowerSheet()
            → _exportInjectFreezeDropdown()（冻结窗格 + 状态列下拉）
            → 下载 xlsx（共 4 个 Sheet）
```

**排期三步曲**（最需要先理解的部分）：

| 步 | 函数 | 回答什么问题 | 改它会影响 |
|---|---|---|---|
| 1 | `expandedFlowNodes` | 这个需求**要做哪些环节** | 环节多了少了、强制文学前置 |
| 2 | `runDecisionChain` | 优先级多高、谁审批 | 优先分、档位、审批人 |
| 3 | `autoSchedule` | 每个环节**什么时候做** | 起止日期、人力冲突、超 DDL 判定 |

---

## 7. 常见维护任务（How-To）

> 改完任何功能后，**务必用 Node 做一次整文件语法校验**（见 §8 坑位①），再交给用户。

### 7.1 改某产出项工期
- 用户已在「设置」页可改（`state.config.itemTime`）
- 改默认值：`DEFAULT_ITEM_TIME`（搜 `const DEFAULT_ITEM_TIME`），升级时记得 +`_cfgVersion`

### 7.2 改管线 / 审批人
- `DEFAULT_CONFIG.pipelines`（搜 `pipelines:`）：改 name/score/importance/approver
- 或直接「设置」页改

### 7.3 改排期流程节点
- `DEFAULT_CONFIG.flows`（搜 `flows:`），或用「设置 → 流程编辑器」可视化改
- 节点字段：`type`(make/review/archive/sync)、`item`(工期取自工时表)、`days`(固定天数)、`qty`、`ownerRule`、`reviewerRule`、`litExpand`(文学展开)

### 7.4 更新法定假期
- `DEFAULT_CONFIG.holidays`（搜 `holidays:`）：按"单周放假>3天则扣该周"规则录入 `{weeks:[...], name, rule}`
- 仅影响导出重排与容量计算

### 7.5 改优先级/重要度分档
- `prioThresholds` / `importanceThresholds`（搜 `prioThresholds`）

### 7.6 改人员 / 人力上限
- `people`（搜 `people:`）、`headcount`（搜 `headcount:`，含 lit/sceneArt/charArt 人数）

### 7.7 副本需求字段（已实现的解析规则）
- 问卷第 14 题 `是否需要添加需求副本`（含"是/需要"）→ `needCopy`
- 第 15 题 `需求副本信息` 格式：`名称,DDL`；多份用 `;` 或换行分隔，如 `1jjj,2026-12-05`
- 解析在 `parseQuestionnaireMultiCol` + `parseCopyInfo`；副本继承主需求全部字段，仅改 name/ddl，标 `_copyOf`，副本项在队列显黄色「副本」徽标
- **注意**：这是 CSV/问卷导入路径的副本；表单入口的"需求数量+副本DDL"尚未实现（两套独立机制）

### 7.8 新增问卷字段识别
1. 在 `QUESTIONNAIRE_HEADER_MAP` 加 `表头别名 → 字段key`
2. 在 `parseQuestionnaireMultiCol` 的字段分发里加对应 `else if (key === 'xxx')` 分支
3. 必要时写解析小函数（参考 `parseCopyInfo`/`normalizeDDL`）
4. 在 `renderPendingQueue` 决定队列里怎么展示

### 7.8b 智能表格（腾讯文档·智能表）字段识别 —— v75 新增

**需求收集渠道已从腾讯问卷切到智能表格**，两种格式的解析器并存、自动嗅探。
入口链路：`detectImportFormat` → `parseSmartsheetTable`。

智能表格与问卷的**三点本质差异**（改解析逻辑前必看，否则会重犯已修过的 bug）：

| | 腾讯问卷 | 智能表格 |
|---|---|---|
| 表头 | 一行 | **两行**：第 1 行合并单元格「组名」，第 2 行才是字段名 |
| 列名 | 带 `1.` 编号、带 `:[Bible设定] xxx` 后缀 | 干净的字段名，无编号无后缀 |
| 语义 | 表头带项名，单元格打勾 | **表头是分类，单元格写项名**（方向相反） |

**列语义判定优先级固定为「字段名 > 数据内容 > 组名」，不要改回组名优先。**
组名是合并单元格，只有首列有值、向右延续时会越界；行长度也常错位
（曾因 `trim()` 吃掉前导空列导致整行左移一格，分类列被判成管线列）。
组名只在字段名说不清时用（`Bible设定` 既可能是已有产出、也可能是特殊产出需求），
**且要求字段名也能被 `findAssetMid` 认出**，否则交给 `sniffColumnKind` 值嗅探裁决。

加新字段的步骤：
1. 简单一对一字段（需求名/DDL/提需人）→ 往 `SMARTSHEET_FIELD_MAP` 加一条
2. 需要按组名区分语义的 → 改 `smartsheetGroupKind` 的正则
3. 新的产出物二级分类 → 加到 `ASSETS_TREE`，`findAssetMid` 会自动认
4. 新的子分类写法 → **只往 `CATEGORY_SUB_ALIASES` 加别名**（见 7.9）
5. 改完必跑 `node verify_smartsheet.js`

两个必须保留的坑位防护，别删：

- **`SMARTSHEET_JUNK_FIELD`** —— 过滤智能表格里没重命名的默认字段
  （`单选` / `多选 2` / `日期 1` 等）。这些列常残留测试值，
  不过滤会被值嗅探当成真实产出列，凭空多出产出物。
- **`readXlsxFile` 里 aoa 必须「截断 + 补齐」到统一宽度** —— 组名行比字段行短时，
  只截断不补齐会让 `tableToTabText` 按 header 长度把数据行右侧切掉，
  而 **DDL / 提需人正好在最右边**，会静默丢失。

还有一个易踩的陷阱：**判列名不能用 `resolvePipelineId`**。
它是给【单元格值】用的模糊子串匹配（`世界观Bible概念` → bible），
拿来判列名会误伤 —— 产出物列「叙事设计」含子串「叙事」（管线名）会被判成管线列，
该列的已有产出整列丢失。判列名要用 `isStrictPipelineName`（精确匹配 + 产出物名优先），
并且 `byField` 里**产出物判定必须排在管线判定之前**。

### 7.9 文学子分类路由规则
- `lit-simple`（简单补充设定）→ 走标准纯文学管线
- `lit-system`（**系统文学设定**）→ 决策链标「⚠ PM 需单独拉起」；
  导出主表名后加 `🔔PM单独拉起`、Sheet2 加「PM单独拉起」列

⚠️ **新增或修改子分类写法时，只往 `CATEGORY_SUB_ALIASES` 加别名，
永远不要改 `CATEGORY_TREE` 的 key。**

`CATEGORY_TREE` 的 key 是**对外规范名**，必须与三处保持一致：
UI 下拉框 `#f_category` 的文案、`config.flows[id].name`、甘特图输出。

历史 bug（2026-08-13，用户实测才发现）：`CATEGORY_TREE` 写的是「系统**性**设定」，
而 UI 下拉和 flows 写的是「系统**文学**设定」。智能表格填后者 → 查表失败 →
`resolveCategory` **静默兜底成 `lit-simple`** → 真实需求「径山书院概念刷新」走错整条流程。

现在的机制：
- `CATEGORY_SUB_ALIASES` 别名表，覆盖 4 套来源（问卷选项／智能表格选项／UI 下拉／口头简称）
- `resolveSubCategoryId(big, raw)` —— 精确→前缀→包含，**按别名长度降序匹配**。
  降序是硬要求：否则「重点NPC」会被「NPC」抢走而错判成 `char-normal`（有断言盯死）
- `canonicalSubName(big, raw)` —— 把值归一到规范名再存进 record，
  否则下游按字符串比较子分类的地方会漏判

**顺带一条通用原则**：任何「查表失败就 `return` 默认值」的兜底都要警惕 ——
它会把配置不一致藏起来，最后只能靠用户肉眼发现。

### 7.10 配置升级（权威数据变更必做）
- 凡改动 `flows`/`holidays`/`pipelines`/`*Thresholds` 等**内置权威数据**，
  必须 `DEFAULT_CONFIG._cfgVersion` +1
- `loadState()` 会在 `_cfgVersion` 不一致时，用最新内置版**强制覆盖**这些字段（保留用户的需求/任务/工时/阈值微调）

---

## 8. 已知坑 & 注意事项（必读）

1. **⚠️ 改完必须语法校验**：曾因 `parseQuestionnaireMultiCol` 结尾出现两段 `return`（重复 `return records; }`），导致第 3 个 `<script>` 块报 `Illegal return statement`，**整页 JS 崩溃、界面全空**（数据其实在 localStorage 没丢，强刷即可恢复）。
   - 校验命令（在文件目录运行）：
     ```bash
     node -e 'const fs=require("fs"),vm=require("vm");const h=fs.readFileSync("ai-scheduling-board-v10.html","utf8");const re=/<script\b[^>]*>([\s\S]*?)<\/script>/gi;let m,i=0,b=0;while((m=re.exec(h))){i++;try{new vm.Script(m[1]);}catch(e){b++;console.log("❌ 第"+i+"块:"+e.message);}}console.log("共"+i+"块,错误"+b+"个");'
     ```
   - 必须输出 `共3块,错误0个` 才能交付。

2. **⚠️ 依赖文件同目录**：HTML 用相对路径 `<script src="xlsx-js-style.min.js">` 引入 SheetJS，必须和 HTML 在同一目录，否则 Excel 导入/导出报 `XLSX is not defined`。当前这两个文件只在 `deploy/`，本地根目录缺失——维护时统一放齐。

3. **⚠️ localStorage 单点**：所有数据在一个 key。用户清浏览器缓存/换设备 → 数据全无。交接时务必提醒：**定期用 📤 导出 JSON 备份**。

4. **`重置` 按钮不可逆**：清空 requirements+tasks，只留默认 config。

5. **排期引擎是核心算法**（`autoSchedule`/`_doBatchSchedule`）：改动需谨慎，改后用「✨ 示例」数据回归 导入→排期→导出 全流程。

6. **配置版本号机制**：不升 `_cfgVersion` 的话，老用户浏览器里的旧缓存不会更新权威数据（flows/holidays），表现为"我改了流程但用户那边没变"。

7. **⚠️ 只测底层函数不算测过，必须测真实入口**。
   真实教训（2026-08-13）：修完子分类别名后，直接调 `parseSmartsheetTable` 全绿，
   但走机器人真实入口 `handleMessage` 仍然是错的（那里对文本做了 `trim()`，
   吃掉了组名行的前导空列）。**底层函数通过 ≠ 用户路径通过。**
   所以回归时至少要走一遍「上传文件 → 看右侧表单」的完整路径，
   最省事的办法是用 §9 的 playwright 脚本。

8. **⚠️ 判「列名」和判「单元格值」必须用不同的匹配严格度**。
   值可以模糊匹配（用户填写自由度高），**列名必须精确**——
   列名是结构，猜错会导致整列数据作废。
   已有的一对例子：`resolvePipelineId`（给值用，模糊）
   vs `isStrictPipelineName`（给列名用，精确）。

9. **⚠️ 别写死行号进文档**。行号会随代码改动漂移，
   跳过去看到别的东西比没有行号更误导（实测两份文档 197 处行号全部失效过）。
   需要行号就跑 `node scripts/gen-code-index.js` 重新生成 `代码索引.md`。

10. **甘特图加列要改 4 处**：`LEFT` 数组、`C_xxx` 列索引常量、
    `st.cols` 列宽、`dataValidation.col`。冻结窗格的 `xSplit` 引用 `LEFT.length`
    会自动跟随，不用手改。少改一处的典型症状是「下拉框挂错列」或「冻结线位置不对」。

---

## 9. 回归测试怎么跑（改完代码必做）

三层，从快到慢，**建议全跑**，总共不到 2 分钟。

### 第 1 层：语法校验（3 秒）

见 §8 坑位①的命令，必须输出 `共3块,错误0个`。
这一步能挡住「整页 JS 崩溃、界面全空」这种致命问题。

### 第 2 层：解析逻辑断言（5 秒）

```bash
node verify_smartsheet.js
```

必须输出 `PASS 102 / FAIL 0`（条数会随新增断言增长，只看 `FAIL 0`）。

覆盖内容：真实智能表格文件、多行多选、特殊需求、文学产出、
**问卷老格式回归**（确保新解析器没抢走老格式）、单行表头容错、
勾选式单元格、需求副本展开、子分类别名 12 组、组名行错位 4 种变体、
`isStrictPipelineName` 单元断言。

> 这个脚本用 `vm` 沙箱从 HTML 里抽函数出来测，**不需要浏览器**。
> 它按函数名抓取源码块，所以**如果你重命名了函数，脚本会明确报错告诉你找不到**，
> 而不是静默跳过。

### 第 3 层：浏览器端到端（30 秒）

真实打开页面、真实上传 xlsx、真实读表单的值。**这层是唯一能测出
「上传后表单没填对」这类问题的**，前两层测不出来。

参考 `~/.workbuddy/skills/deploy-verify-with-playwright/SKILL.md`，
核心就是：`page.locator("#aiFileInput").set_input_files(文件)` →
等 3 秒 → 读 `#f_name` / `#f_category` 等字段的值 → 截图。

**判据**：5 条需求的分类/DDL/提需人/管线/产出全对，且 `pageerror` 为 0。

---

## 10. 发布：两条链路都要推

**这是最容易出错的一步。** 项目同时挂了内网和外网两个入口，
只推一边的话，用户会看到旧版然后告诉你「你改的没生效」。
**已经因为这个被发现过两次。**

| 入口 | 地址 | 更新方式 |
|---|---|---|
| 内网 OA Pages | https://ai-scheduling-board.pages.woa.com/ | API `update`（见 §10.1） |
| 外网 GitHub Pages | https://pxjsnd589-gif.github.io/ai-scheduling-board/ | 推 `gh-pages` 分支根目录（见 §10.2） |

**改完代码第一件事**：把主文件同步到 `deploy/index.html`。
两条链路都是从 `deploy/` 目录发布的，不同步就是发旧版。

```bash
cp ai-scheduling-board-v10.html deploy/index.html
diff -q ai-scheduling-board-v10.html deploy/index.html && echo "同步 OK"
```

### 11.1 内网 OA Pages

```bash
node 部署脚本/deploy.js update ai-scheduling-board.pages.woa.com ./deploy index.html,xlsx-js-style.min.js,fflate.min.js
```

只改了 HTML 可以只传它（更快）：把文件列表换成 `index.html`。

> 需要先申请自己的 API Key，见交接说明第四节。
> 站点是 API 模式创建的，**只能用 API 更新，不能用 Git 推**。

### 11.2 外网 GitHub Pages

Pages 服务的是 `gh-pages` 分支的**根目录**，不是 `main`，也不是 `deploy/` 子目录。
所以**推 `main` 不会更新网站**，两个分支都要推。

具体命令见 `~/.workbuddy/skills/ghpages-plumbing-deploy/SKILL.md`
（用 git 底层命令构树，避开了 Windows 上凭据弹窗卡死的问题）。

一个必须校验的点：构完树跑 `git ls-tree -l <树>`，
**确认每个文件的 blob sha 都不一样**。如果有两个相同，说明构树出错了，
推上去会得到一个「index.html 内容变成 JS 库」的死站 —— 而且不报错。

### 11.3 发布后验证（不要跳过）

**别只看命令返回成功、也别只看 HTTP 200。** 必须落盘比对字节：

```bash
curl -s https://ai-scheduling-board.pages.woa.com/ -o live_woa.html
diff -q live_woa.html deploy/index.html && echo "内网一致"

curl -sL https://pxjsnd589-gif.github.io/ai-scheduling-board/ -o live_gh.html
diff -q live_gh.html deploy/index.html && echo "外网一致"
```

> ⚠️ `curl -o /dev/null -w "%{size_download}"` 这种写法会误报 0，
> 看起来像文件是空的，其实不是。**必须落盘 `diff`。**

GitHub Pages 构建需要约 **45 秒**才生效，太早去 curl 会拿到旧版。

浏览器里看的话记得 **Ctrl+Shift+R 强制刷新**，否则是本地缓存。

---

## 11. AI 助手版（skill）—— 改完网页版记得同步

`~/.workbuddy/skills/ai-scheduling/` 是同一套排期规则的 Node 版本，
给 AI 助手在对话里调用。**它和网页版是两份独立代码，改了一边不同步就会漂移。**

需要同步的对应关系：

| 网页版 | skill 里的位置 |
|---|---|
| `generateReqNames` / `litNodeToAsset` | `index.js` 同名函数 |
| 智能表格解析模块 | `parse_import.js` 同名函数 |
| `CATEGORY_TREE` / `CATEGORY_SUB_ALIASES` | `parse_import.js` **和** `index.js` 两处都要改 |
| `isStrictPipelineName` / `buildSmartsheetPlan` 判定顺序 | `parse_import.js` 同名函数 |
| 甘特图 `LEFT` + `C_xxx` 常量 | `export-gantt.js` 同名常量 |

改完必跑 `node selftest.js`，必须 `FAIL=0`（当前 184 项断言）。

> 如果你不打算维护 AI 助手版，可以忽略这一整节 —— 网页版是完全独立的，
> 不依赖 skill。只是别忘了它存在，以后有人问「为什么机器人排的和网页不一样」时能想起来。

---

## 12. 接手 Checklist（导师上手步骤）

**第一阶段：确认能用（10 分钟，不碰代码）**

- [ ] 打开 https://ai-scheduling-board.pages.woa.com/ ，确认能访问
- [ ] 点 **✨ 示例** 加载演示数据，走一遍：录入 → 排期 → 导出 Excel
- [ ] 拿一份真实的智能表格导出 xlsx 上传，确认右侧表单填对了
- [ ] 打开「设置」页，看一眼流程编辑器 / 工时表 / 阈值分别在哪
- [ ] 确认能进管理页 https://pages.woa.com/admin/ai-scheduling-board.pages.woa.com

到这里为止，**如果你只是要让它继续能用，就够了**。下面是要改代码才需要。

**第二阶段：确认能改（30 分钟）**

- [ ] 本地双击打开 `ai-scheduling-board-v10.html`（同目录要有那两个 `.js`）
- [ ] 跑一次 `node scripts/gen-code-index.js`，翻一下 `代码索引.md` 建立地图感
- [ ] 跑一次 `node verify_smartsheet.js`，确认 `FAIL 0`（这是你的安全网）
- [ ] 读 §7 挑一个最简单的改动练手（比如改某个产出项的默认工期）
- [ ] 改完跑 §8① 语法校验 + §9 回归测试，确认都过

**第三阶段：确认能发布（20 分钟，第一次做要慢）**

- [ ] 去 https://pages.woa.com/admin 申请自己的 API Key，设进环境变量
- [ ] `cp ai-scheduling-board-v10.html deploy/index.html`
- [ ] 按 §10.1 推内网，按 §10.3 落盘 diff 验证
- [ ] 按 §10.2 推外网（`gh-pages` 和 `main` 都要推），同样落盘 diff 验证
- [ ] 浏览器 Ctrl+Shift+R 强刷，确认看到的是新版

**最后：告诉业务方**

- [ ] 数据存在浏览器本地，**定期用 📤 导出 JSON 备份**，换设备/清缓存前必须先备份
- [ ] 「重置」按钮不可逆，会清掉所有需求和任务

---

## 13. 一句话交接

> 单文件前端排期工具，数据在浏览器本地。改功能只动 `ai-scheduling-board-v10.html`，
> 改完跑「语法校验 + `verify_smartsheet.js`」两道闸，然后 `cp` 成 `deploy/index.html`，
> **内网外网两条链路都推、都落盘 diff 验字节**。
> 权威配置（流程/假期/阈值）变更要升 `_cfgVersion`。
> 排期引擎和表格解析这两块别凭感觉改，先看 §7 和 §8 的坑位说明。
