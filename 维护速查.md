# 维护速查（遇到问题先翻这一页）

> 配套 `排期工具_维护手册.md`（细节）和 `代码索引.md`（函数行号）。
> 这一页只解决「我该动哪里」，不解释原理。

---

## 一、业务方来提需求时

| 他说 | 你要动的地方 | 要不要升 `_cfgVersion` |
|---|---|---|
| 某个环节工期不对 | 「设置」页工时表就能改，不用碰代码 | 改代码里 `DEFAULT_ITEM_TIME` 才需要 |
| 要加/删一个制作环节 | 「设置 → 流程编辑器」可视化改 | 改代码里 `DEFAULT_CONFIG.flows` 才需要 |
| 换审批人 / 改管线分值 | 「设置」页改 `pipelines` | 改代码默认值才需要 |
| 明年的法定假期要更新 | 代码 `DEFAULT_CONFIG.holidays` | **要** |
| 优先级档位线要调 | 「设置」页 `prioThresholds` | 改代码默认值才需要 |
| 制作人数变了 | 「设置」页 `headcount` | 改代码默认值才需要 |
| **需求表格加了一列** | 见下面「二」 | 不用 |
| **新增一个需求子分类** | 见下面「三」 | **要**（动到 flows） |
| 甘特图要加一列 | 见下面「四」 | 不用 |

> 一半以上的需求**根本不用改代码**，「设置」页就能解决。先确认这一点，能省很多事。

**`_cfgVersion` 是什么**：在 `DEFAULT_CONFIG` 里，改动内置权威数据
（`flows` / `holidays` / `pipelines` / 阈值）后必须 +1。
不升的话，用户浏览器里缓存的旧配置不会被覆盖，
症状是「我明明改了，业务方那边还是旧的」。

---

## 二、需求表格加了一列，怎么让工具认出来

先判断这一列是什么性质：

| 列的性质 | 举例 | 怎么做 |
|---|---|---|
| 一对一的简单字段 | 「期望上线时间」「需求来源」 | 往 `SMARTSHEET_FIELD_MAP` 加一条 `'列名': '字段key'` |
| 新的产出物二级分类 | 「动效设计」 | 加进 `ASSETS_TREE`，`findAssetMid` 会自动认 |
| 新的管线 | 「短剧线」 | 加进 `DEFAULT_CONFIG.pipelines`，**要升 `_cfgVersion`** |
| 新的分类写法 | 「系统概念」（其实就是系统文学设定） | 加进 `CATEGORY_SUB_ALIASES`，见「三」 |

改完**必跑**：

```bash
node verify_smartsheet.js      # 必须 FAIL 0
```

然后真实上传一次表格，看右侧表单填对没有。**光跑脚本不够**——
有过「脚本全绿但真实上传是错的」的先例（原因是入口处对文本做了 trim）。

---

## 三、新增/修改需求子分类

**只往 `CATEGORY_SUB_ALIASES` 加别名，不要改 `CATEGORY_TREE` 的 key。**

```javascript
// 正确做法：加别名
const CATEGORY_SUB_ALIASES = {
  '纯文学类': {
    'lit-system': ['系统文学设定', '系统性设定', '系统概念', /* ← 在这里加新写法 */]
  }
};
```

如果是真的要加一个**新子分类**（不是别名），要动 4 个地方，一个都不能漏：

1. `CATEGORY_TREE` —— 加 `'规范名': 'new-id'`
2. `CATEGORY_SUB_ALIASES` —— 加 `'new-id': ['规范名', ...其它写法]`
3. `DEFAULT_CONFIG.flows` —— 加 `'new-id': { name: '规范名', nodes: [...] }`，**升 `_cfgVersion`**
4. HTML 里的下拉框 `#f_category` —— 加 `<option value="new-id">大类 / 规范名</option>`

**这四处的名称必须完全一致。** 不一致的后果不是报错，而是
`resolveCategory` 静默兜底到默认分类，需求走错整条流程，只能靠人肉发现。
（2026-08-13 真实踩过，「径山书院概念刷新」被从系统文学设定误判成简单补充设定。）

改完在浏览器里实测：选这个新分类 → 排期 → 看环节对不对。

---

## 四、甘特图加一列

在 `exportExcelGantt` 里，**必改 4 处**：

1. `LEFT` 数组 —— 加列标题
2. `C_xxx` 列索引常量 —— 加一个，后面的常量值全部 +1
3. `st.cols` —— 加列宽
4. `dataValidation.col` —— 如果状态列位置变了要跟着改

冻结窗格的 `xSplit` 引用 `LEFT.length`，会自动跟随，不用管。

漏改的典型症状：下拉框挂在错误的列上，或冻结线位置不对。

---

## 五、改完代码的固定流程

**四步，别跳**：

```bash
# ① 语法校验（3 秒）—— 挡住"整页白屏"
node -e 'const fs=require("fs"),vm=require("vm");const h=fs.readFileSync("ai-scheduling-board-v10.html","utf8");const re=/<script\b[^>]*>([\s\S]*?)<\/script>/gi;let m,i=0,b=0;while((m=re.exec(h))){i++;try{new vm.Script(m[1]);}catch(e){b++;console.log("❌ 第"+i+"块:"+e.message);}}console.log("共"+i+"块,错误"+b+"个");'

# ② 回归断言（5 秒）—— 必须 FAIL 0
node verify_smartsheet.js

# ③ 浏览器实测 —— 双击 HTML，上传真实表格，看表单
#    这一步不能省，前两步测不出"上传后填错"

# ④ 刷新代码索引
node scripts/gen-code-index.js
```

然后才是发布。

---

## 六、发布（两条链路，都要推）

**最容易犯的错：只推一边。** 已经因此被业务方发现两次「你改的没生效」。

```bash
# 第 0 步，最容易忘：同步到 deploy/
cp ai-scheduling-board-v10.html deploy/index.html

# 内网
node 部署脚本/deploy.js update ai-scheduling-board.pages.woa.com ./deploy index.html

# 外网：推 gh-pages 分支根目录（推 main 不会更新网站）
# 命令见 ~/.workbuddy/skills/ghpages-plumbing-deploy/SKILL.md

# 验证：必须落盘 diff，不能只看 HTTP 200
curl -s https://ai-scheduling-board.pages.woa.com/ -o live1.html
diff -q live1.html deploy/index.html && echo "内网 OK"

curl -sL https://pxjsnd589-gif.github.io/ai-scheduling-board/ -o live2.html
diff -q live2.html deploy/index.html && echo "外网 OK"
```

> GitHub Pages 要等约 45 秒才生效，太早 curl 拿到的是旧版。
> 浏览器看要 **Ctrl+Shift+R** 强刷。

---

## 七、出问题时的排查顺序

| 症状 | 先查什么 |
|---|---|
| 页面全白 | 语法错误。跑「五①」的语法校验；数据没丢，在 localStorage 里 |
| Excel 导入/导出报错 | `XLSX is not defined` → 两个 `.js` 没和 HTML 放在同一目录 |
| 上传表格后字段填错 | 跑 `node verify_smartsheet.js` 看是哪条断言红了 |
| 上传后某列数据完全丢失 | 大概率是列语义判错。看 `buildSmartsheetPlan` 的判定结果 |
| 分类判成了默认值 | 查 `CATEGORY_SUB_ALIASES` 有没有这个写法（见「三」） |
| 改了配置但业务方那边没变 | `_cfgVersion` 忘记 +1 |
| 线上还是旧版 | 忘了 `cp` 到 `deploy/`，或只推了一条链路 |
| 业务方说数据全没了 | 清缓存/换设备导致。**提醒他们定期用 📤 导出 JSON 备份** |

---

## 八、有个前提要说清楚

这个工具**不会自己坏**。它是纯静态页面，没有后端、没有数据库、没有定时任务、
没有依赖的外部服务。挂在内网 OA Pages 上，服务负责人已确认站点会长期保留。

所以**「维护」不是日常工作**，只在两种情况下才需要动：

1. 业务规则变了（假期、工期、流程、分类）
2. 需求收集表格的结构变了

平时不用管它。如果只是让它继续能用，你什么都不需要做。
